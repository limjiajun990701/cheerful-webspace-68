import React from "react";
import SkillsRowsSection from "./SkillsRowsSection";

// This wrapper is to keep import path and About structure unchanged.
const DynamicSkillsSection = () => {
  return <SkillsRowsSection />;
};

export default DynamicSkillsSection;
